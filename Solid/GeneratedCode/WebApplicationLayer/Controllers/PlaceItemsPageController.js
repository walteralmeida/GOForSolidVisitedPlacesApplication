////////////////////////////////////////////////////////////////////////////////////////////
// This is Generated Code
// You should not modify this code as it may be overwritten. Use Partial classes instead
// Generated By Generative Objects  
//////////////////////////////////////////////////////////////////////////////////////////// 

(function () {
	// 
	Solid.Web.Controllers.PlaceItemsPageController = function(applicationController) {
		var self = this;
		this.subscriptions = [];
		// store all subscriptions in this array, to unsubscribe on release.
		
		this._objectType = "PlaceItemsPage";

		this.applicationController = applicationController;
		this.ObjectsDataSet = applicationController.ObjectsDataSet;
		this.contextId = [this.applicationController.getNextContextId()];
		this.customController = undefined;

		// Integrate custom code if any
		if (Solid.Web.Controllers.PlaceItemsPageControllerCustom !== undefined) {
		    this.customController = new Solid.Web.Controllers.PlaceItemsPageControllerCustom(self);
		};

		this.pageTitle = ko.observable("Places Page");

		// Initialize View Models and Data Stores
		
			this.PlaceItemsViewModel = new Solid.Web.ViewModels.PlaceGridViewModel(this, $("#PlaceGrid"), null, null, this.contextId);		
	
		// Attach to view models events
		this.subscriptions.push(this.PlaceItemsViewModel.StatusData.IsBusy.subscribe( function (newValue) { self.OnPlaceItemsViewModelIsBusyChanged(newValue); }));
		
		this.IsInEditMode = function() {

			return  (self.PlaceItemsViewModel.StatusData.DisplayMode && self.PlaceItemsViewModel.StatusData.DisplayMode() == 'edit');
		};


		// Events Handlers
		this.OnPlaceItemsViewModelIsBusyChanged = function (newValue) {
		};

		this.initialize = function() {
			if (Solid.Web.Controllers.Custom && Solid.Web.Controllers.Custom.getPageTitle) {
				self.pageTitle(Solid.Web.Controllers.Custom.getPageTitle(self));
			}

	
			// Call custom initialize if defined
			if (self.customController !== undefined && self.customController.initialize !== undefined) {
			    self.customController.initialize();
			}
		// Initial data load for all source elements (no dependencies)
			if (!GO.Filter.hasUrlFilter(self.PlaceItemsViewModel.FILTER_NAME, self.PlaceItemsViewModel)) {
				self.PlaceItemsViewModel.LoadPlaceObjectCollection();
			}
		};
		this.initialize();



		this.release = function() {
			// unsubscribe
			for(var i = 0;i <  self.subscriptions.length;i++) {
				self.subscriptions[i].dispose();
			}
			self.subscriptions = [];
			self.ObjectsDataSet.cleanContext(self.contextId);
			
			self.PlaceItemsViewModel.release();
			self.PlaceItemsViewModel = null;
		};


		return self;
	};		
	
	if (window.ApplicationSourceHandler)
		window.ApplicationSourceHandler.onSourceLoaded("/Controllers/PlaceItemsPageController.js");
} ());
