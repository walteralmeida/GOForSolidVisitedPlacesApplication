////////////////////////////////////////////////////////////////////////////////////////////
// This is Generated Code
// You should not modify this code as it may be overwritten. Use Partial classes instead
// Generated By Generative Objects  
//////////////////////////////////////////////////////////////////////////////////////////// 

(function () {
	// 
	Solid.Web.Controllers.CountryItemsPageController = function(applicationController) {
		var self = this;
		this.subscriptions = [];
		// store all subscriptions in this array, to unsubscribe on release.
		
		this._objectType = "CountryItemsPage";

		this.applicationController = applicationController;
		this.ObjectsDataSet = applicationController.ObjectsDataSet;
		this.contextId = [this.applicationController.getNextContextId()];
		this.customController = undefined;

		// Integrate custom code if any
		if (Solid.Web.Controllers.CountryItemsPageControllerCustom !== undefined) {
		    this.customController = new Solid.Web.Controllers.CountryItemsPageControllerCustom(self);
		};

		this.pageTitle = ko.observable("Countries Page");

		// Initialize View Models and Data Stores
		
			this.CountryItemsViewModel = new Solid.Web.ViewModels.CountryGridViewModel(this, $("#CountryGrid"), null, null, this.contextId);		
	
		// Attach to view models events
		this.subscriptions.push(this.CountryItemsViewModel.StatusData.IsBusy.subscribe( function (newValue) { self.OnCountryItemsViewModelIsBusyChanged(newValue); }));
		
		this.IsInEditMode = function() {

			return  (self.CountryItemsViewModel.StatusData.DisplayMode && self.CountryItemsViewModel.StatusData.DisplayMode() == 'edit');
		};


		// Events Handlers
		this.OnCountryItemsViewModelIsBusyChanged = function (newValue) {
		};

		this.initialize = function() {
			if (Solid.Web.Controllers.Custom && Solid.Web.Controllers.Custom.getPageTitle) {
				self.pageTitle(Solid.Web.Controllers.Custom.getPageTitle(self));
			}

	
			// Call custom initialize if defined
			if (self.customController !== undefined && self.customController.initialize !== undefined) {
			    self.customController.initialize();
			}
		// Initial data load for all source elements (no dependencies)
			if (!GO.Filter.hasUrlFilter(self.CountryItemsViewModel.FILTER_NAME, self.CountryItemsViewModel)) {
				self.CountryItemsViewModel.LoadCountryObjectCollection();
			}
		};
		this.initialize();



		this.release = function() {
			// unsubscribe
			for(var i = 0;i <  self.subscriptions.length;i++) {
				self.subscriptions[i].dispose();
			}
			self.subscriptions = [];
			self.ObjectsDataSet.cleanContext(self.contextId);
			
			self.CountryItemsViewModel.release();
			self.CountryItemsViewModel = null;
		};


		return self;
	};		
	
	if (window.ApplicationSourceHandler)
		window.ApplicationSourceHandler.onSourceLoaded("/Controllers/CountryItemsPageController.js");
} ());
