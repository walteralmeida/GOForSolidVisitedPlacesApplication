////////////////////////////////////////////////////////////////////////////////////////////
// This is Generated Code
// You should not modify this code as it may be overwritten. Use Partial classes instead
// Generated By Generative Objects  
//////////////////////////////////////////////////////////////////////////////////////////// 
(function () {
	// 
    Solid.Web.ViewModels.NoticeBoxViewModel = function (htmlContent, title, isSticky) {
        var self = this;

        // Popups management
        this.viewLoader = new  Solid.Web.Views.viewLoader();

        this.noticeId = "notice" + $("#purr-container").children().size();
        this.title = title;
        this.htmlContent = htmlContent;
        this.isSticky = (isSticky) ? isSticky : false;

        this.showAsMessageBox = function (type) {
            $.jGrowl.defaults.position = 'center';
            $.jGrowl.defaults.closerTemplate = '<div>[ Tout fermer ]</div>';
			var img;
            switch (type) {
                case 'error':
                    img = "http://cdn1.iconfinder.com/data/icons/prettyoffice/32/alert.png";
                    break;
                case 'info':
                    img = "http://cdn1.iconfinder.com/data/icons/lullacons/large-info.png";
                    break;
                default:
                    //Unknowned type
                    img = "";
                    break;
            }
            self.title = "<img src='" + img + "'/>" + self.title;
            $.jGrowl(htmlContent, { header: self.title, sticky: self.isSticky, theme: 'default', position: 'top'});
        };
    };
}());